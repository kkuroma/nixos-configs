# Templates: option-defining modules (schemas only — implementations live in
# parts/modules/ and parts/services/). Auto-imported; unused options cost nothing.
{ imports = import ../../lib/import-dir.nix ./.; }
